import 'dart:ui';

class AppColors {
  // Primary color used for app elements
  static const primaryColor = Color(0xff4e7465);

  // Main text color
  static const textColor = Color(0xFF213A57);

  // Hint text color
  static const textHintColor = Color.fromARGB(149, 33, 58, 87);

  // Button color for primary actions
  static const buttonColor = Color(0xFF81b29a);

  // Background color for circular elements (like cards)
  static const circleColor = Color.fromARGB(136, 159, 209, 185);

  // Secondary background color
  static const secondaryColor = Color(0xFFf4f1de);

  // Color for navigation elements
  static const navigationColor = Color(0xFFf2cc8f);

  // Color indicating cancellation actions
  static const cancelColor = Color(0xFFe07a5f);

  // Color representing uncompleted tasks
  static const uncompletedColor = Color(0xFFd6542c);

  // White color for backgrounds or elements
  static const whiteColor = Color(0xFFffffff);

  // Gradient colors used in the app
  static const List<Color> colorGradient = <Color>[
    Color(0xFF8db2a1), // First gradient color
    Color(0xFFc4d9ca), // Second gradient color
  ];
}
